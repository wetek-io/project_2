## Code Style Guidelines

- Follow PEP 8 for all Python code.
- Use `Black` to format the code before pushing.
- Run `flake8` to check for linting errors.
- Write tests for new features and ensure they pass `pytest`.
